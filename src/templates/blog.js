import React from 'react';

import Layout from '../components/layout';

const Blog = () => {
  return (
    <Layout>
      <p>This is the blog template.</p>
    </Layout>
  );
};

export default Blog;
